const notificationRepo = require('./notification.repo')
const userNotificationRepo = require('./user-notification.repo')
const userRepo = require('./user.repo')

module.exports = {
  notificationRepo,
  userNotificationRepo,
  userRepo
}
